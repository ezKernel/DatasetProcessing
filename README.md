# This project contains some useful python scripts for data processing. 

# 1.RemoveEXIF.py
Remove EXIF info for images.
Functions are:
(1) def fix_orientation(image_path, output_path=None) # Remove EXIF info for one image.
(2) def batch_fix(folder) # Remove EXIF info for images in an folder.

# ConvertImages2jpg.py
Convert images to .jpg format.
